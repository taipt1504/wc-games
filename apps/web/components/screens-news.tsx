'use client';
/* GOLAZO — News · Article (stub — ported in fan-out from screens-news.jsx) */
import type { ScreenProps } from '@/lib/store';
import { Stub } from '@/components/screen-stub';

export function News(_: ScreenProps) {
  return <Stub title="News" />;
}
export function Article(_: ScreenProps) {
  return <Stub title="Article" />;
}
