'use client';
/* GOLAZO — Admin console (stub — ported in fan-out from screens-admin.jsx). FULLBLEED route. */
import type { ScreenProps } from '@/lib/store';
import { Stub } from '@/components/screen-stub';

export function Admin(_: ScreenProps) {
  return <Stub title="Admin console" />;
}
