'use client';
/* GOLAZO — Leaderboard · My bets · Wallet · Profile (stub — ported in fan-out from screens-compete.jsx) */
import type { ScreenProps } from '@/lib/store';
import { Stub } from '@/components/screen-stub';

export function Leaderboard(_: ScreenProps) {
  return <Stub title="Leaderboard" />;
}
export function MyBets(_: ScreenProps) {
  return <Stub title="My bets" />;
}
export function Wallet(_: ScreenProps) {
  return <Stub title="Wallet" />;
}
export function Profile(_: ScreenProps) {
  return <Stub title="Profile" />;
}
