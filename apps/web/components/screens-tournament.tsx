'use client';
/* GOLAZO — Teams · Team detail · Groups · Bracket (stub — ported in fan-out from screens-tournament.jsx) */
import type { ScreenProps } from '@/lib/store';
import { Stub } from '@/components/screen-stub';

export function Teams(_: ScreenProps) {
  return <Stub title="Teams" />;
}
export function TeamDetail(_: ScreenProps) {
  return <Stub title="Team" />;
}
export function Groups(_: ScreenProps) {
  return <Stub title="Groups" />;
}
export function Bracket(_: ScreenProps) {
  return <Stub title="Bracket" />;
}
